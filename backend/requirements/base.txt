Django>=5.2.9,<5.3
djangorestframework==3.15.2
gunicorn==23.0.0
django-redis==5.4.0
# channels_redis 4.2.0 与 redis-py >=5.1/8.x 的 asyncio 连接池不兼容（长连接下
# group_add 会卡死报 "Timeout reading from redis"）。固定到 5.0.1 可修复 WS 握手。
redis==5.0.1
mysqlclient==2.2.7
PyMySQL==1.1.1
python-dotenv==1.2.2
drf-spectacular==0.28.0
drf-spectacular[sidecar]
djangorestframework-simplejwt==5.5.1
celery==5.5.1
django-celery-beat==2.8.0
django-celery-results==2.6.0
django-cors-headers==4.7.0
django-prometheus==2.3.1
Pillow==12.3.0
PyJWT==2.13.0
cryptography>=42.0
pycryptodome>=3.20.0
boto3
django-storages>=1.14.2
stripe>=9.0.0,<10.0.0
python-magic>=0.4.27
# 邮件 HTML 白名单消毒（ammonia Rust 绑定），见 utils/html_sanitize.py
nh3>=0.2.18
channels[daphne]==4.2.0
channels_redis==4.2.0
SQLAlchemy>=2.0,<3
PySocks>=1.7.1
# ── 2C4G 资源约束：协程模型 + 连接池 ──
# gevent：Gunicorn 协程 worker，2 核下用协程而非线程承载高并发 I/O
gevent==24.11.1
# 注：MySQL 连接池（2C4G 资源优化）原计划钉死 dj-db-conn-pool==2.3.0，但该包在 PyPI /
# 阿里云镜像均不存在（正确包名为 django-db-connection-pool，最新 1.2.6，且需验证与
# Django 5.2 + gevent 的兼容性）。当前生产镜像（v1.0.2）亦未启用连接池，运行稳定，
# 故暂不钉死该依赖；待选型并测试兼容后再单独加回，避免引入未经验证的 DB 驱动变更。
