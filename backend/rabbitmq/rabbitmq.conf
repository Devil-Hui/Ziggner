# RabbitMQ 3.12 — 2C4G / 容器约 192–256MB
# 管理插件占内存；生产建议关闭 management 镜像若仅用 5672
vm_memory_high_watermark.relative = 0.45
disk_free_limit.absolute = 50MB
# 告知 broker 可用内存约 180MB，避免默认按主机总内存高估
total_memory_available_override_value = 180000000
# 降低消费者默认预取，避免任务堆积占内存
consumer_timeout = 1800000
